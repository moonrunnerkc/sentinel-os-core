# Core dependencies
numpy>=1.24.0
scipy>=1.10.0
pyyaml>=6.0
jsonschema>=4.0.0
aiofiles>=23.0.0
psutil>=5.0.0

# Cryptography (PQ fallback via ed25519, real PQ via liboqs when available)
cryptography>=42.0.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-timeout>=2.1.0
pytest-benchmark>=4.0.0
hypothesis>=6.0.0

# Static analysis
flake8>=6.0.0
mypy>=1.0.0
bandit>=1.7.0

# LLM (optional - for cognitive reasoning)
llama-cpp-python>=0.2.0

# Physics simulation (optional - install separately if needed)
# mujoco>=3.0.0
# pybullet>=3.2.0

# Neuromorphic (optional - install separately if needed)
# brian2>=2.5.0

# Homomorphic encryption (optional - install separately if needed)
# tenseal>=0.3.0

# Post-quantum (optional - install via pip install liboqs-python)
# liboqs-python>=0.7.0

# SMT solver for formal verification (optional but recommended)
z3-solver>=4.12.0
